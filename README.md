# tcp-status-client
A TCP/IP client application that connects to a server, acknowledges incoming commands, simulates hardware processing delay, reports UI button status, and handles busy-state responses.
